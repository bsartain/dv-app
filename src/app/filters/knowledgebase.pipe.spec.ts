import { KnowledgebasePipe } from './knowledgebase.pipe';

describe('KnowledgebasePipe', () => {
  it('create an instance', () => {
    const pipe = new KnowledgebasePipe();
    expect(pipe).toBeTruthy();
  });
});
